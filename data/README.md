# Source data

This directory is intentionally populated at analysis time from the public source repository:

- https://github.com/fsndzomga/efficient-benchmarking-ai-agents
- file: `data/all_leaderboards_costs_HAL.csv`

The frozen local CSV is used only for reproducible computation. Before public release, verify the upstream data license and either retain the attribution and permitted copy or replace it with a download script plus a recorded content hash.
